# Project-wide Gradle settings.

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX
android.useAndroidX=true

# Kotlin code style
kotlin.code.style=official

# La línea deprecated se elimina — el default actual ya es 'true', no hace falta declararla
# android.usesSdkInManifest.disallowed=false   ← ELIMINADA

# Mejora de rendimiento incremental
android.nonTransitiveRClass=true
